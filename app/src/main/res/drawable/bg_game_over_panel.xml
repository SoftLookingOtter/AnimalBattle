<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- I’m using a soft cream background here so the panel feels warm and calm. -->
    <solid android:color="#FDF7F0" />

    <!-- Rounded corners to match the rest of the UI style. -->
    <corners android:radius="24dp" />

    <!-- Small padding inside the shape so the contents get some breathing room. -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp" />

    <!-- A very subtle stroke to give the panel a bit of definition. -->
    <stroke
        android:width="1dp"
        android:color="#E0D0C0" />

</shape>
