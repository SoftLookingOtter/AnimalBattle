<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- This is the base theme I use for the whole app. -->
    <style name="Base.Theme.AnimalBattle" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- I can override colors here later if I want a custom look. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- My main theme just extends the base one for now. -->
    <style name="Theme.AnimalBattle" parent="Base.Theme.AnimalBattle" />

</resources>
